FROM python:3.9-slim


RUN apt-get update && apt-get install -y \
    libgtk-3-0 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app


RUN pip install --no-cache-dir flet==0.21.0 psycopg2-binary

COPY . .


EXPOSE 8501

CMD ["python", "app.py"]